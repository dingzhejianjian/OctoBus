{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "username": {
      "type": "string",
      "description": "Default login username for the TopSec EDR management center (typically admin)."
    },
    "password": {
      "type": "string",
      "description": "Default login password (plaintext; the service will AES-encrypt it before sending to EDR)."
    },
    "apiToken": {
      "type": "string",
      "description": "Optional pre-obtained API token for direct API access without login."
    }
  }
}
