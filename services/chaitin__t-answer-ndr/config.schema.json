{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "endpoint": {
      "type": "string",
      "format": "uri",
      "description": "Management platform base URL, for example https://t-answer.example.com."
    },
    "timeoutMs": {
      "type": "integer",
      "minimum": 1,
      "default": 5000,
      "description": "JSON-RPC request timeout in milliseconds."
    },
    "skipTlsVerify": {
      "type": "boolean",
      "default": false,
      "description": "Pass OctoBus-compatible per-request TLS verification bypass options to the runtime."
    },
    "maxDownloadBytes": {
      "type": "integer",
      "minimum": 1,
      "maximum": 134217728,
      "default": 33554432,
      "description": "Maximum downloaded file size accepted in memory."
    }
  },
  "required": [
    "endpoint"
  ]
}
