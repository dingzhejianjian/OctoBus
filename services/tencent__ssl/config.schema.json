{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "endpoint": {
      "type": "string",
      "format": "uri",
      "default": "https://ssl.tencentcloudapi.com",
      "description": "Tencent Cloud SSL endpoint; override only for an approved proxy or test server."
    },
    "region": {
      "type": "string",
      "description": "Tencent Cloud region (SSL API uses ap-guangzhou by default)."
    },
    "timeoutMs": {
      "type": "integer",
      "minimum": 1,
      "maximum": 120000,
      "default": 10000,
      "description": "HTTP timeout in milliseconds."
    },
    "skipTlsVerify": {
      "type": "boolean",
      "default": false,
      "description": "Disable TLS certificate verification only for controlled development endpoints."
    }
  }
}
