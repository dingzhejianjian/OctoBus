{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "secretId": {
      "type": "string",
      "description": "Tencent Cloud SecretId. Request secret_id/secretId still takes precedence when provided.",
      "x-octobus-smoke": "smoke-secret-id"
    },
    "secretKey": {
      "type": "string",
      "description": "Tencent Cloud SecretKey. Request secret_key/secretKey still takes precedence when provided.",
      "x-octobus-smoke": "smoke-secret-key"
    },
    "token": {
      "type": "string",
      "description": "Optional temporary STS token."
    }
  },
  "required": ["secretId", "secretKey"]
}
